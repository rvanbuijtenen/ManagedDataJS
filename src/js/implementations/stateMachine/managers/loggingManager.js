import * as logging from "../../../framework/basicDataManager/basicDataManager.js"
export class LoggingDataManager extends bdm.BasicDataManager {
	constructor(schema, logFunction, ...mixins);
	super(schema, mixins);
}